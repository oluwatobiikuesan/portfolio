# xAI API key — obtain from https://console.x.ai/
# Set this as an environment variable on your hosting platform (e.g. Render).
# Never commit the real key to source control.
XAI_API_KEY=your_xai_api_key_here

# (Optional) Port for the Express server. Defaults to 3000.
# PORT=3000
