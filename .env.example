# CORS origin for the Vite dev server.
# Set this to your production URL when deploying (e.g. https://xdoodle.onrender.com).
# Leave unset to allow all origins (suitable for local development).
VITE_CORS_ORIGIN=
